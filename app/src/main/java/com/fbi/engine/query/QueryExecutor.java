package com.fbi.engine.query;

import com.fbi.engine.domain.query.Query;
import com.project.bi.general.Executor;

/**
 * Executes a query and returns a result
 */
public interface QueryExecutor extends Executor<Query> {
}
